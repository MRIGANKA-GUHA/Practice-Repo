//SETUP
npm init to start the backend
We have to set type "module" for "import" statement and "commonjs" for "require" statement
dev: "node index.js"

//PRETTIER AND GITIGNORE
For installing prettier We use this line "npm install --save-dev --save-exact prettier"
For writing prettier "npx prettier . --write"
For telling which things to be edited we make a file .prettierrc and to tell which files to ignore .prettierignore
Telling git to ignore files we use .gitignore

//AUTO RESTARTING THE SERVER
To install nodemon we use this command "npm install --save-dev nodemon"
"dev": "nodemon index.js",
    "start": "node index.js"
we have to change the package.json , start is used for the production and we used node for production and nodemon for development
 
//DOT-ENV INSTALL
We do it buy this command line "npm i dotenv"
import dotenv from "dotenv"

//FILE STRUCTURE
We make a file Public where we store images
src folder in which we have controllers,db,middlewares,models,routes,utils,validators

//EXPRESS INSTALL
By writing this command "npm install express --save" express installed
Basic config should be done like urlencoded, taking the req, static

//INSTALLING CORS
"npm  i cors"

//API HANDLING
For apiResponse we create a file in which we take statusCode,message,data and export 
Same we take apiError in which we extends a class Error which gives all the utilities of error and make a structure of it

//CONSTANTS
It is the constants of the project in which we add roles that will be constant and if we want to extend any other roles in future we can do that also
the progress in the project

//DB CONNECTION
We first npm install mongoose then we make a file database connection in which we connect the db in a try catch as it can take time 
and in env we will declare the url of mongodb. we also change the index.js that if it can connect db then only it can connect to the production
and the bakend will start

